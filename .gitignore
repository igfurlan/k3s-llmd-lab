# Vagrant machine state (machine IDs, generated SSH keys)
.vagrant/
*.log

# Never commit cluster credentials — k3s.yaml embeds admin certs,
# and the node-token is a join secret.
kubeconfig
*kubeconfig*
k3s.yaml
node-token
.k3s-token

# Virtual disks, should they ever land in the project folder
*.vdi
*.vmdk
*.box

# Editor / OS noise
.vscode/
.idea/
Thumbs.db
desktop.ini

# Cards built for the write-up's images — screenshot sources, not part of the lab.
# Kept locally only.
docs/post-cards.html
